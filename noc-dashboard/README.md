# NOC Dashboard (static web)

장비·인터페이스 상태를 한눈에 보는 다크 테마 실시간 운영 대시보드. (데모 데이터)

## 실행
별도 빌드 없이 `index.html`을 브라우저로 열면 됩니다. (또는 `npx serve .`)

## 교체 포인트
- `app.js`의 `fetchDevices()`를 본인 모니터링 API 호출로 교체하세요.
